# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
env/
venv/
ENV/

# Project Specific
.env
error.log
images/
output_images/
.agent/
.agents/
_agent/
_agents/
.superpowers/

# OS
.DS_Store
Thumbs.db

# Firebase Web config with real API key (user-local, never committed)
firebase-config.js

# One-time setup script (local only, not part of repo history)
LAPTOP_COMMIT_PUSH.sh

# Nested repository (content-pipeline plugin, managed separately)
content-pipeline/
